// Re-export the derived print geometry so print modules import from one place.
export { DPI, CARD_IN, BLEED_IN, TRIM_PX, BLEED_PX, BLEED_MARGIN } from "@/config/constants";

export const SCREEN_WIDTH = 500;
export const PT_PER_IN = 72;
